import { SettingsPlaceholderPage } from "@/components/settings-placeholder-page";

export default function SettingsInAppPage() {
  return (
    <SettingsPlaceholderPage
      title="In-app settings"
      description="Configure in-app message defaults and display rules."
    />
  );
}
