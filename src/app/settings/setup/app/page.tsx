import { SettingsPlaceholderPage } from "@/components/settings-placeholder-page";

export default function SettingsAppPage() {
  return (
    <SettingsPlaceholderPage
      title="App settings"
      description="Mobile app SDK keys, push registration, and app identifiers."
    />
  );
}
